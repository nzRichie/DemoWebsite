README For Compx222 Assignment 1.

The @property used in style.css may not validate:

This is because it is an experimental CSS functionality. It works on most browsers, but even if it doesn't work on a specific browser that has no impact on the functionality of the web page. It simply means the glowing border around the main content won't be animated.

This means that the custom defined property --gradient-angle also will not validate. Again, if a specific browser does not support this, it has no impact on the functionality of the website.
